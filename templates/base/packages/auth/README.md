# @repo/auth

Shared email/password auth client for apps.

- httpOnly cookie session (`credentials: "include"`) — no tokens in JS storage
- TanStack Query hooks + TanStack Form sign-in/sign-up
- `RequireAuth` / `RequireRole` stubs
- axe smoke on `SignInForm`
